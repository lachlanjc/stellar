<p id="notice"><%= notice %></p>

<% if @discussion.pinned? %>
<div class="pinned-large">
  <%= inline_svg("pin.svg") %>
</div>
<% end %>
<h1 class="m1-btm ib"><%= @discussion.title %></h1>
<%= link_to "Edit", edit_discussion_path(@discussion), class: "btn btn-sm float-right" %>

<%= render @discussion.comments.order(created_at: :asc) %>

<h2>Comment:</h2>
<%= render "comments/form" %>
</div>
